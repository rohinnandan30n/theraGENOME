FROM python:3.11-slim

WORKDIR /app

# Copy and install requirements
COPY requirements_hackathon.txt ./requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Copy app code
COPY api_server.py .
COPY dashboard.html .

# Expose port
EXPOSE 8000

# Run
CMD ["python", "api_server.py"]
